# ==============================================================================
# TLS Certificate Configuration for local development
# ==============================================================================

tls:
  certificates:
    # Wildcard certificate for *.localhost
    - certFile: /etc/certs/localhost.crt
      keyFile: /etc/certs/localhost.key

    # Wildcard certificate for *.test
    - certFile: /etc/certs/test.crt
      keyFile: /etc/certs/test.key

  stores:
    default:
      defaultCertificate:
        certFile: /etc/certs/localhost.crt
        keyFile: /etc/certs/localhost.key

  options:
    default:
      minVersion: VersionTLS12
      cipherSuites:
        - TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
        - TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305
        - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
